{
  "name": "gbf-crew-portal",
  "version": "1.0.0",
  "description": "Firestore Rules test for gbf-crew-portal",
  "scripts": {
    "test": "firebase emulators:exec --project demo-gbf-meron-portal-rules --only firestore \"node tests/firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.0",
    "firebase-tools": "^13.16.0"
  }
}
